# Lyzr API Key (required for agent calls)
LYZR_API_KEY=your-lyzr-api-key-here

# Optional: Agent ID (can also be set per-component)
AGENT_ID=your-agent-id-here

# Development Mode (optional)
# Set to 'true' to always use mock data instead of API calls
# Useful for testing UI without consuming API credits
NEXT_PUBLIC_USE_MOCK_DATA=false

# Note: When LYZR_API_KEY is missing or API returns 429 (credits exhausted),
# the system will automatically fall back to mock data regardless of this setting.
